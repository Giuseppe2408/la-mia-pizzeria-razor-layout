@{
    Layout = "_RazorLayout"; 
}



<h1>Razor snack</h1>

<h1>1. Dichiarare una variabile valorizzandola con il nostro nome.
Dichiarare un'altra variabile valorizzandola con il nostro cognome.
Stampare il nostro nome e cognome all'interno di un tag h2.</h1>

@{
    string nome = "Giuseppe";
    string cognome = "Acito";
}

<h2>Risultato: @nome @cognome</h2>

<h1>2. Creare una funzione che stampa la somma dei numeri che vanno da 1 a n (con n passato come parametro - n incluso)
Stampare dentro uno span il risultato.</h1>

@{
    int numRand = new Random().Next(1, 91);
    int risultatoSomma = Somma(numRand);



    int Somma(int n)
    {
        return n + n;
    }
}

<span>Risultato: @risultatoSomma</span>

<h1>3. Contare quante volte la lettera a è presente nalla frase "nel mezzo del cammin di nostra vita" e stampare questo valore in pagina</h1>

@{
    string frase = "nel mezzo del cammin di nostra vita";

    int conteggioLettera = Conteggio();

    int Conteggio()
    {
        int somma = 0;
        for(int i = 0; i < frase.Length; i++)
        {

            if(frase[i] == 'a'){
               somma = somma + 1;
            }
        }
        return somma;
    }
}

<div> nella frase nel mezzo del cammin di nostra vita ci sono @conteggioLettera a</div>

<h1>4. Creare una List<int> di 20 numeri interi (generati random da 1 a 90, 1 e 90 inclusi) e mostrare in pagina solo i numeri compresi tra 20 e 70</h1>

@{
    
    List<int> numeriRandom = new List<int>();

    for (int i = 0; i < 20; i++) 
    {
        int randNum = new Random().Next(1, 91);
        numeriRandom.Add(randNum);
    }

    
}

<h1> Ecco i numeri stampati da 20 a 70</h1>

    @foreach (int numero in numeriRandom)
    {
        if (numero > 19 && numero < 71)
        {
            <ul>
                <li>@numero</li>
            
            </ul>
        }
    }
    



<h1>5. Stampare in pagina la data e l'ora attuale (usando la classe DateTime) con il formato anno-mese-giorno ore:minuti:secondi</h1>

    @{
        DateTime oraAttuale = DateTime.Now;
    }

    <h2>Data e ora attuale: @oraAttuale.ToString("yyyy/mm/dd hh:mm:ss")</h2>



<h1>
    6. Partendo dalla data di oggi (usando la classe DateTime) stampare in pagina
        a. la data di 7 giorni fa
        b. che giorno della settimana è oggi (il risultato dovrà essere il nome del giorno (Lunedì, Martedì, ...)
        c. che giorno della settimana sarà nello stesso giorno e mese di oggi ma tra un anno (il risultato dovrà essere il nome del giorno (Lunedì, Martedì, ...)
</h1>

@{
    DateTime today = DateTime.Today;

    DateTime settimanaPrima = today.AddDays(-7);

    DateTime giornoFuturo = today.AddYears(1);
}

    <h2>7 giorni fa era @settimanaPrima.ToString("yyyy/MM/dd")</h2>
    <h2>oggi è @today.ToString("dddd")</h2>
    <h2>fra un anno sarà @giornoFuturo.ToString("dddd")</h2>